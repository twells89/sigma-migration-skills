# tableau / orders-executive-compiler

Sanitized recorded-live release anchor. No tenant IDs, URLs, SQL, or customer
data are committed; the evidence pins the compiler’s minimum coverage, strict
numeric parity, anchor, nonempty-tile, and visual-similarity results.

## Expectations

```json
{
  "artifacts": [
    {"path": "evidence.json", "format": "json"},
    {"path": "checks.sh", "format": "text"}
  ]
}
```
